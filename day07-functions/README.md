# Day 07 — Functions

## What I'm covering today
- Why functions exist (DRY principle, abstraction)
- Defining functions, parameters, default arguments, return values
- Local vs global scope

## Things to remember
- return sends a value back; print() just displays it — they are not the same
- Variables inside a function don't exist outside it unless returned

## Practice for today
See `practice.md` in this folder.

## After today
Fill in `journal.md` with what was actually learned, in your own words — written as if explaining it to a student.
