# Day 03 — First Contact with Python

## What I'm covering today
- IDE setup and what an IDE actually does for you
- print(), comments, basic syntax, why indentation matters in Python
- Variables, naming conventions, dynamic typing
- Core data types: int, float, str, bool
- input() and type conversion

## Things to remember
- Python uses indentation instead of braces to define blocks
- Variables don't need a declared type — Python infers it

## Practice for today
See `practice.md` in this folder.

## After today
Fill in `journal.md` with what was actually learned, in your own words — written as if explaining it to a student.
